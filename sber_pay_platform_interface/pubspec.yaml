name: sber_pay_platform_interface
description: A common platform interface for the sber_pay plugin.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: 2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
